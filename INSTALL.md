# Быстрая установка Sigma Private

## Шаг 1: Установка зависимостей

```bash
npm install
```

## Шаг 2: Сборка расширения

```bash
npm run build
```

Vite соберёт проект за несколько секунд ⚡ и создаст папку `dist/` со всеми необходимыми файлами.

## Шаг 3: Загрузка в Chrome

1. Откройте Chrome и перейдите на `chrome://extensions/`
2. Включите **"Режим разработчика"** (переключатель в правом верхнем углу)
3. Нажмите **"Загрузить распакованное расширение"**
4. Выберите папку `dist/` из проекта
5. Готово! Расширение установлено ⚙️

## Режим разработки

Для мгновенной пересборки при изменении файлов ⚡:

```bash
npm run dev
```

Vite будет следить за файлами и пересобирать за ~200-500ms!

После изменений обновляйте расширение в Chrome:
- Откройте `chrome://extensions/`
- Нажмите кнопку обновления рядом с Sigma Private
- Изменения применятся мгновенно!

## Решение проблем

### Ошибка при сборке
- Убедитесь, что Node.js версии 18+ установлен
- Попробуйте удалить `node_modules/` и запустить `npm install` снова

### Расширение не отображается
- Проверьте, что вы выбрали папку `dist/`, а не корневую папку проекта
- Убедитесь, что сборка прошла успешно (проверьте наличие файлов в `dist/`)

### Иконки не отображаются
- Иконки можно добавить позже
- Расширение будет работать с иконками по умолчанию

## Следующие шаги

1. **Настройка API ключа**: Откройте расширение и добавьте API ключ в настройках
2. **Тестирование**: Попробуйте отправить сообщение в чат
3. **Контекст страницы**: Включите кнопку 📄 для использования контекста текущей страницы

---

По всем вопросам смотрите полную документацию в `README.md`

