# Как перезагрузить расширение после пересборки

После выполнения `npm run build` необходимо обновить расширение в Chrome:

## Метод 1: Через страницу расширений

1. Откройте **chrome://extensions/**
2. Убедитесь, что включен **"Режим разработчика"** (Developer mode) в правом верхнем углу
3. Найдите **Sigma Private**
4. Нажмите кнопку **"Обновить"** (🔄) рядом с расширением

## Метод 2: Полная перезагрузка

1. Откройте **chrome://extensions/**
2. Нажмите **"Удалить"** у расширения
3. Нажмите **"Загрузить распакованное расширение"**
4. Выберите папку `dist`

## Метод 3: Через терминал (автоматизация)

После сборки можно автоматически перезагружать:

```bash
# Сборка + сообщение о перезагрузке
npm run build && echo "Обновите расширение в chrome://extensions/"
```

## Проверка, что изменения применились

1. Откройте DevTools в sidepanel (F12)
2. Во вкладке **Network** проверьте, что загружается `sidepanel.js` и `sidepanel.css`
3. Проверьте размер файлов - они должны совпадать с файлами в `dist/`
4. В консоли проверьте отсутствие ошибок загрузки CSS

## Если стили все равно не применяются

1. Очистите кэш расширения:
   ```
   Shift + Ctrl + Delete → Очистить кэш
   ```

2. Проверьте, что в DevTools → Elements видны классы типа:
   ```css
   <button class="_button_paodf_1 _primary_paodf_25">
   ```

3. Проверьте, что CSS-переменные определены:
   ```javascript
   getComputedStyle(document.documentElement).getPropertyValue('--text-neutral-primary')
   // Должно вернуть: "#ffffffff" или подобное
   ```

## Development mode

Для автоматической перезагрузки можно использовать:

```bash
npm run dev
```

Это запустит watch mode, но все равно потребуется ручное обновление расширения в Chrome после каждого изменения.

## Hotkeys

- `Ctrl/Cmd + R` - перезагрузить sidepanel (только контент, не весь extension)
- `Ctrl/Cmd + Shift + R` - жесткая перезагрузка с очисткой кэша


