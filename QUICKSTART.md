# ⚡ Быстрый старт Sigma Private

## За 3 минуты от нуля до работающего расширения

### Шаг 1: Установка (30 сек)

```bash
cd /Users/aleksejkalina/code/ai-swat/sigma-private
npm install
```

### Шаг 2: Сборка (5 сек ⚡)

```bash
npm run build
```

Vite соберёт проект за пару секунд и создаст папку `dist/` со всеми файлами расширения.

### Шаг 3: Загрузка в Chrome (1 мин)

1. Откройте Chrome
2. Перейдите на `chrome://extensions/`
3. Включите **"Режим разработчика"** (переключатель справа сверху)
4. Нажмите **"Загрузить распакованное расширение"**
5. Выберите папку `dist/` из этого проекта
6. ✅ Готово!

### Шаг 4: Тест (30 сек)

1. Кликните на иконку расширения в панели Chrome
2. Появится окно чата
3. Попробуйте отправить сообщение
4. Получите placeholder ответ (настоящий AI пока не подключен)

### Шаг 5: Разработка (опционально)

Для режима разработки с мгновенной пересборкой ⚡:

```bash
npm run dev
```

Vite будет следить за изменениями и пересобирать за ~200-500ms!

После изменений:
- Откройте `chrome://extensions/`
- Нажмите кнопку обновления рядом с расширением
- Изменения применятся мгновенно!

## Что дальше?

### Подключить настоящий AI:

1. Получите API ключ от OpenAI/Anthropic
2. Откройте `src/utils/api.ts`
3. Раскомментируйте код API запроса
4. Добавьте свой ключ в настройках расширения

### Добавить новый компонент:

```tsx
// src/popup/components/MyComponent.tsx
import React from 'react';

interface MyComponentProps {
  title: string;
}

const MyComponent: React.FC<MyComponentProps> = ({ title }) => {
  return <div>{title}</div>;
};

export default MyComponent;
```

### Изменить стили:

Отредактируйте `src/popup/popup.css` - изменения применятся после пересборки.

## Полезные команды

```bash
# Сборка для продакшена
npm run build

# Разработка с watch
npm run dev

# Очистка сборки
npm run clean

# Проверка кода
npm run lint

# Форматирование
npm run format
```

## Проблемы?

### "Module not found"
```bash
rm -rf node_modules
npm install
```

### "Build failed"
```bash
npm run clean
npm run build
```

### Расширение не работает
- Проверьте консоль браузера (F12)
- Проверьте, что выбрана папка `dist/`, а не корневая
- Попробуйте перезагрузить расширение

## Документация

- 📖 [README.md](README.md) - Полная документация
- 🏗️ [ARCHITECTURE.md](ARCHITECTURE.md) - Архитектура проекта
- ⚛️ [REACT_MIGRATION.md](REACT_MIGRATION.md) - React интеграция
- ⚡ [VITE_MIGRATION.md](VITE_MIGRATION.md) - Vite миграция
- 🚀 [INSTALL.md](INSTALL.md) - Детальная установка

---

*Omnissiah guides your code! ⚙️*

