# Архитектура Sigma Private

## Обзор

Sigma Private - это Chrome расширение, построенное на Manifest V3 с использованием TypeScript. Расширение состоит из трёх основных компонентов, которые взаимодействуют через систему сообщений Chrome.

## Компоненты

### 1. Popup (src/popup/)

**Назначение**: Пользовательский интерфейс чата

**Файлы**:
- `popup.html` - структура интерфейса
- `popup.css` - стили (современный тёмный дизайн)
- `popup.ts` - логика управления UI и взаимодействия

**Функциональность**:
- Отображение истории чата
- Отправка сообщений пользователя
- Переключение режима контекста страницы
- Переключение режима перевода
- Сохранение/загрузка истории чата из Chrome Storage

### 2. Background Service Worker (src/background/)

**Назначение**: Центральный обработчик логики расширения

**Файлы**:
- `background.ts` - основной service worker

**Функциональность**:
- Обработка сообщений от popup и content scripts
- Запросы к AI API
- Координация получения контекста страницы
- Управление переводами
- Хранение настроек

**Типы сообщений**:
- `CHAT_REQUEST` - запрос на обработку сообщения чата
- `GET_PAGE_CONTEXT` - запрос контекста текущей страницы
- `TRANSLATE_TEXT` - запрос на перевод текста

### 3. Content Script (src/content/)

**Назначение**: Взаимодействие с веб-страницами

**Файлы**:
- `content.ts` - скрипт, внедряемый в каждую страницу

**Функциональность**:
- Извлечение текстового контента страницы
- Получение выделенного текста
- Очистка и форматирование контента
- Обработка динамических изменений DOM

## Поток данных

```
┌─────────────┐
│   Popup UI  │
│  (popup.ts) │
└──────┬──────┘
       │ chrome.runtime.sendMessage()
       ▼
┌─────────────────────────┐
│   Background Worker     │
│   (background.ts)       │
└────┬────────────────┬───┘
     │                │
     │ AI API         │ chrome.tabs.sendMessage()
     │                ▼
     │          ┌──────────────┐
     │          │Content Script│
     │          │ (content.ts) │
     │          └──────────────┘
     │                │
     │                │ Извлекает данные
     │                ▼
     │          ┌──────────────┐
     │          │  Web Page    │
     └──────────┴──────────────┘
```

## Типы и интерфейсы (src/types/)

Централизованные TypeScript типы для всего проекта:

- `MessageType` - enum типов сообщений
- `Message` - базовый интерфейс сообщения
- `PageContext` - структура контекста страницы
- `ChatMessage` - структура сообщения чата
- `TranslationRequest/Response` - интерфейсы для перевода
- `ChatRequest/Response` - интерфейсы для чата

## Утилиты (src/utils/)

**api.ts**: Сервисы для взаимодействия с внешними API

- `AIService` - класс для работы с AI API
- `getAIConfig()` - получение конфигурации API

## Система сборки

**Vite** ⚡ компилирует TypeScript и создаёт три основных bundle:

1. `popup.js` - код для popup интерфейса (React app)
2. `background.js` - код service worker
3. `content.js` - код для content script

**Преимущества Vite:**
- ⚡ 10-15x быстрее Webpack
- 🔥 Мгновенный Hot Module Replacement
- 📦 Умный code splitting
- 🎯 Нативный ESM

**Custom plugin** копирует статические файлы:
- manifest.json
- popup.html
- public/icons

## Хранение данных

**Chrome Storage Local**:
- `chatHistory` - история последних 50 сообщений
- `settings` - настройки (API ключ, модель, язык)

## Разрешения (Permissions)

- `activeTab` - доступ к активной вкладке
- `tabs` - управление вкладками
- `storage` - локальное хранилище
- `scripting` - внедрение скриптов
- `<all_urls>` - доступ ко всем страницам для контекста

## Будущие улучшения

1. **Options Page** - страница настроек для конфигурации API
2. **Context Menu** - быстрый перевод через контекстное меню
3. **Side Panel** - боковая панель для постоянного чата
4. **Offline Mode** - кэширование и работа без интернета
5. **Multiple AI Providers** - поддержка разных AI сервисов
6. **Custom Prompts** - пользовательские промпты

## Безопасность

- API ключи хранятся в Chrome Storage (изолированное хранилище расширения)
- Content Security Policy (CSP) через Manifest V3
- Минимальные необходимые разрешения
- Нет передачи данных третьим сторонам (кроме выбранного AI провайдера)

